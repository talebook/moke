# Moke 功能调研与改进建议

> 调研日期：2026-07-18  
> 调研范围：Moke 前端、Tauri 宿主、内嵌 Readest、仓库内最新 Talebook 参考服务端、扩展系统及同类产品公开能力。

## 1. 结论摘要

Moke 已经具备一款 Talebook 桌面客户端的核心骨架：服务器连接、访问码与账号认证、书架、书库、网络书源、搜索、详情、批量操作、离线下载、内嵌 Readest、自动更新、墨水屏模式，以及正在形成的扩展系统。

下一阶段最重要的工作不是重新开发阅读器功能，而是把以下三层真正连接起来：

1. **Talebook**：书籍、账号、阅读状态、精确进度、上传、推送和统计的数据源。
2. **Moke**：统一入口、任务管理、跨端状态、离线管理和设备管理。
3. **Readest**：阅读、进度、书签、标注、笔记、TTS、翻译和词典等专业阅读能力。

推荐优先顺序：

1. 修复已确认的交互和兼容性问题。
2. 接通 Readest 与 Talebook 的精确阅读进度。
3. 建立完整的离线下载管理中心。
4. 使用 Talebook 原生接口重做个人阅读数据与统计。
5. 完成移动端适配、上传和设备推送。
6. 再推进多服务器、智能书架和扩展生态。

## 2. 当前能力盘点

### 2.1 已具备的 Moke 能力

- 连接用户自建 Talebook 服务器。
- 支持 HTTP、HTTPS、自签名证书和局域网地址。
- 支持访问码、登录、注册和密码重置。
- 书架、书库、搜索、详情和个人历史页面。
- 按作者、标签、出版社、格式等浏览和筛选。
- 网格、列表和表格视图，支持持久化视图偏好。
- 右键菜单、长按菜单、批量选择和批量下载。
- 离线下载到 IndexedDB；Tauri 下同时保存到本地磁盘。
- 内嵌 Readest，并在独立阅读窗口打开文件。
- 墨水屏模式和慢刷新设备自动检测。
- 桌面端自动更新。
- Windows、macOS、Linux、Android 和 iPadOS 构建流程。
- 扩展发现、启停、卸载、权限声明、侧边栏入口、后台进程和阅读器事件。

### 2.2 最新 Talebook 已提供、但 Moke 尚未充分利用的能力

- ~~收藏：`/api/favorites`、`/api/book/{id}/favorite`。~~开发者注：与下一个接口是一样的
- 书架：`/api/shelf`、`/api/book/{id}/shelf`。
- 在读与读完：`/api/reading`、`/api/read-done`。
- 阅读状态：`/api/book/{id}/readstate`。
- **精确阅读进度**：`/api/book/{id}/progress`。
- 阅读统计：`/api/reading/stats`。
- 普通上传、分片上传和分片合并。
- 邮件推送和发送到阅读设备。
- 用户设备管理。
- OPDS、WebDAV 和网络书源相关能力。

其中精确进度接口已经允许客户端保存不超过 8 KB 的自定义进度结构，并记录更新时间，见 [`BookReadingProgress`](CODE_NEED/talebook/webserver/handlers/book.py#L2309)。这使跨设备续读可以在不新增服务端接口的情况下实施。

### 2.3 Readest 已具备、无需在 Moke 重复开发的能力

- EPUB、PDF、MOBI、AZW3、FB2、CBZ、TXT 等格式阅读。
- 书签、划线、标注和笔记。
- TTS、有声朗读、翻译、在线与离线词典。
- 阅读主题、字体、版式和墨水屏阅读设置。
- 双书并排、阅读辅助、快捷键和手柄操作。
- OPDS、KOSync、Readwise、Hardcover 等集成。
- 本地或云端同步阅读进度、书签、标注和统计。

Moke 应重点做好宿主桥接、状态映射、入口设计和中文引导。

## 3. 已确认的问题（已全部完成）

### P0-1：搜索格式筛选没有实际生效（已完成）

搜索页维护了 `activeFilter`，EPUB、PDF、MOBI、TXT 按钮只改变选中样式，没有参与请求参数或前端结果过滤，见 [`src/app/search/page.tsx`](src/app/search/page.tsx#L42)。

建议：

- 优先在客户端按 `book.files` 过滤，立即修复交互承诺。
- 后续在服务端搜索接口增加格式参数，以减少大结果集传输。
- 在筛选后显示真实数量，并处理一本书含多个格式的情况。

验收标准：切换格式后结果、数量、空状态和批量选择范围同时变化。

### P0-2：移动端仍使用固定桌面布局（已完成）

[`DesktopLayout`](src/components/layout/DesktopLayout.tsx) 固定预留 220px 侧边栏；已有 [`TabBar`](src/components/layout/TabBar.tsx) 未被任何页面使用，且包含当前不存在的 `/network` 路由。

建议：

- 桌面端保留侧边栏，平板窄屏使用可折叠侧栏，手机使用底部导航。
- 详情页将固定左右栏改为窄屏上下布局。
- 页面边距、表格列、批量操作栏和弹窗适配安全区域。
- Android/iPadOS 发布前加入真实设备截图和触控回归检查。

验收标准：360px、768px、1024px 和桌面宽度下无横向溢出，主要操作可单手完成。

### P0-3：服务端能力缺少显式协商（已完成）

Moke 会连接用户自行维护、版本不一的 Talebook 实例。精确进度、分片上传、WebDAV、书架等能力在旧版本中可能不存在。

建议：

- 建立 `ServerCapabilities` 模型。
- 优先读取服务端返回的版本和功能开关；必要时对关键端点做一次轻量探测。
- 功能不可用时隐藏入口或显示升级提示，而不是让请求直接失败。
- 将所有版本差异集中在 API 适配层，页面只依赖统一能力接口。

验收标准：连接新版、旧版和缺少权限的服务端时，页面均能给出明确且可恢复的状态。

### P1-1：网络异常和接口错误经常被静默忽略（已完成）

多个页面存在空 `catch {}`，断网、登录过期、权限不足和服务端不兼容可能都表现为空列表。

建议：

- 统一网络错误、HTTP 错误和 Talebook 业务错误的类型。
- 提供页面级错误状态、重试按钮和重新登录入口。
- 检测 `navigator.onLine`，增加离线横幅。
- 对搜索等快速变化请求使用取消机制，防止旧请求覆盖新结果。

### P1-2：“全文搜索”描述与实际能力不一致（已完成）

当前搜索主要基于书名、作者、标签、简介等元数据，不是书籍正文内容索引。

建议：

- 近期将产品文案改为“书库搜索”或“元数据搜索”。
- 中长期再评估服务端正文索引、Readest 单书搜索和本地离线索引。

## 4. 全部功能建议

以下工作量使用：`S`（数天）、`M`（约 1～3 周）、`L`（跨模块或需要长期迭代）。

### 4.1 精确阅读进度与跨端续读（已完成）

**优先级：P0　工作量：M　服务端状态：已具备接口**

Readest 已经通过 [`readerStore`](readest/apps/readest-app/src/store/readerStore.ts#L445) 上报页码、总页数、百分比、fraction、章节和 section href，Talebook 也已提供 `/api/book/{id}/progress`。

建议实现：

- 打开阅读器时传入 `serverUrl`、Talebook `bookId` 和格式等宿主上下文。
- 建立 Talebook 书籍 ID 与 Readest 文件 hash 的稳定映射。
- 阅读过程中节流保存精确位置，窗口关闭时强制刷新最后一次进度。
- 打开书籍前读取远端进度，并与本地 Readest 进度比较更新时间。
- 冲突时提供“使用本机进度 / 使用服务器进度”，避免静默覆盖。
- 首次翻页自动标记为“在读”。
- 达到可配置阈值时提示标记“读完”，同时保留手动状态切换。
- 书架首页增加“继续阅读”区域和进度条。
- 无网络时写入本地待同步队列，恢复连接后自动上传。

建议进度结构：

```json
{
  "schema": 1,
  "format": "epub",
  "fraction": 0.425,
  "chapter": "第十二章",
  "section_href": "chapter12.xhtml",
  "cfi": "epubcfi(...) ",
  "page": 126,
  "total_pages": 310,
  "updated_at": "2026-07-18T10:00:00Z",
  "device_id": "..."
}
```

验收标准：两台设备轮流阅读同一本书时，能够恢复到合理位置，并且旧设备不会覆盖更新进度。

### 4.2 阅读状态、收藏与个人书架体系

**优先级：P1　工作量：S～M　服务端状态：已具备接口**

建议：

- 统一“收藏、书架、未读、在读、读完、已下载”状态模型。
- 详情页允许直接修改阅读状态。
- 书架页增加收藏、在读、读完和已下载视图。
- 使用 `/api/favorites`、`/api/reading`、`/api/read-done` 获取完整列表。
- 右键菜单和批量操作支持标记在读、读完、收藏和取消收藏。
- 操作使用乐观更新，失败时回滚并提示原因。

### 4.3 个人阅读统计

**优先级：P1　工作量：S～M　服务端状态：基础统计已具备**

当前历史页通过有限历史记录逐本查询阅读状态，存在请求数量和数据完整性问题。

建议：

- 使用 `/api/reading/stats` 展示当前在读、累计读完、本月在读和本月读完。
- 增加最近阅读、最近读完和继续阅读卡片。
- 将“历史记录”和“阅读状态”分开，避免统计重复计数。
- 中长期增加阅读时长、页数、连续阅读天数和年度热力图。
- 增加常读作者、标签、系列、格式和语言分布。
- 支持按时间范围筛选并导出 JSON/CSV。

阅读时长和页数需要 Moke/Readest 采集；若要跨设备统一，需扩展服务端统计模型。

### 4.4 离线下载管理中心

**优先级：P1　工作量：M～L**

当前 [`offline-books.ts`](src/lib/offline-books.ts) 只能按服务器和书籍查询单条记录，没有全量管理、任务队列、空间统计和恢复机制。

建议：

- 新增“下载管理”页面：下载中、已下载、失败、暂停。
- 支持批量暂停、继续、取消、重试和删除。
- 显示速度、已用空间、预计剩余时间和磁盘剩余空间。
- 支持断点续传、Range 请求和下载完整性校验。
- 存储键升级为“服务器 + 书籍 + 格式”，支持同一本书多个格式。
- 磁盘路径使用服务器 ID、书籍 ID、格式分层，避免同名书覆盖。
- 保存前清理旧格式或明确保留策略，防止孤立文件。
- Tauri 下不要同时在 IndexedDB 和磁盘保存完整副本，可在 IndexedDB 只保存索引和元数据。
- 删除本地文件时同步更新 Talebook 的下载状态。
- 检测服务端文件更新，提示重新下载。
- 允许配置下载目录，支持打开所在文件夹。

### 4.5 Web 模式离线阅读与能力对齐

**优先级：P1　工作量：M**

当前 Web 模式可以把 Blob 存入 IndexedDB，但阅读入口只支持 Tauri 文件路径。

建议：

- 从 IndexedDB 读取 Blob，创建对象 URL 并交给 Web 版 Readest。
- 为离线应用增加 PWA manifest、Service Worker 和静态资源缓存。
- 明确 Web 与 Tauri 的能力差异，并在 UI 中隐藏不支持的操作。
- 检测浏览器配额，提示持久化存储授权和清理策略。
- 对跨域、Cookie 和服务器 CORS 提供专门诊断页。

### 4.6 上传、导入和书库维护

**优先级：P1　工作量：M　服务端状态：普通与分片上传均已具备**

建议：

- 支持拖拽文件到书库页面上传。
- 多文件上传队列，显示逐文件进度和总体进度。
- 超过阈值后自动使用 `/api/book/upload/chunk` 和 `/api/book/upload/complete`。
- 支持取消、重试、分片重传和会话恢复。
- 上传后轮询导入/扫描状态并自动刷新书库。
- 展示重复书籍、格式合并、元数据解析失败等结果。
- 管理员可编辑元数据、封面、可见范围和删除格式。
- 本地文件可选择“只在 Readest 打开”或“上传到 Talebook 后管理”。

### 4.7 邮件与阅读设备推送

**优先级：P1　工作量：M　服务端状态：已具备接口**

建议：

- 详情页和批量操作栏增加“发送到设备”。
- 设置页管理用户设备和默认设备。
- 支持 Kindle 邮箱、Boox、墨案等服务端已支持的设备类型。
- 推送前选择格式，必要时请求服务端转换。
- 显示推送任务状态、成功/失败原因和历史记录。
- 支持一键重试和批量推送。

### 4.8 多服务器配置与快速切换

**优先级：P1　工作量：M**

当前 [`server.ts`](src/lib/store/server.ts) 只保存一个服务端。

建议：

- 保存多个 Talebook 实例的名称、地址、图标、账号和最后连接时间。
- 侧边栏提供快速切换器。
- 每个实例独立保存 Cookie、访问码、离线书籍、阅读进度缓存和筛选偏好。
- 支持设置默认服务器、编辑、重新认证和移除。
- 展示连接延迟、服务端版本和健康状态。
- 支持导入、导出服务器配置；导出时默认不包含凭证。
- 可选实现局域网服务发现。

### 4.9 智能书架与自定义集合

**优先级：P2　工作量：M**

建议预置动态书架：

- 继续阅读。
- 最近加入。
- 最近阅读。
- 未读、在读、已读完。
- 已收藏、已下载。
- 长期未打开。
- 本月读完。
- 随机选一本。

建议允许用户将以下条件组合并保存：

- 作者、标签、出版社、系列、评分、语言和格式。
- 阅读状态、下载状态和收藏状态。
- 加入日期、阅读日期、出版日期和文件大小。
- AND/OR 条件、排序方式和最大条数。

### 4.10 搜索与发现能力

**优先级：P2　工作量：M～L**

建议分三层实施：

1. **近期**：修复格式筛选，增加作者、标签、系列、出版社和状态筛选，保存最近搜索。
2. **中期**：搜索建议、拼音/模糊匹配、搜索历史、热门搜索和组合过滤。
3. **长期**：服务端正文全文索引、离线书籍正文索引、标注和笔记搜索。

还可增加：

- 相同作者、系列或标签的相关推荐。
- 随机发现和“很久没读”的书。
- 网络书源结果与本地书库结果统一展示，并清楚标记来源。
- 搜索结果去重和“已在书库”状态提示。

### 4.11 网络书源体验

**优先级：P2　工作量：M**

建议：

- 保存常用书源和最近浏览分类。
- 展示书源健康状态、响应时间和最后成功时间。
- 网络搜索支持取消、超时、分来源增量返回和部分失败提示。
- 保存网络书籍时显示抓取、章节、转换和入库进度。
- 对同名、同 ISBN 或相同来源 URL 做去重提示。
- 管理员可在 Moke 中启停、测试和排序书源。

### 4.12 OPDS、WebDAV 与外部阅读器

**优先级：P2　工作量：M**

Talebook 已支持 OPDS 和 WebDAV，Moke 可提供更友好的配置入口：

- 一键复制个人 OPDS 地址和认证说明。
- 展示 WebDAV 挂载地址和平台教程。
- 生成 KOReader、Readest、KyBook 等常见客户端配置指引。
- 提供连接测试和权限诊断。
- 中长期评估 Kobo 双向同步，但不建议作为近期主线。

### 4.13 扩展系统产品化

**优先级：P2　工作量：L**

当前扩展系统已经具备较好的技术基础，但应用内只管理已放入 AppData 的扩展。

建议按以下顺序推进：

1. 应用内安装本地扩展包。
2. 扩展更新和 API 版本兼容检查。
3. 包哈希、数字签名和发布者身份。
4. 首次启用时展示权限与风险。
5. 权限变化后重新授权。
6. 扩展日志、崩溃状态、重启和资源占用。
7. 后台进程超时、内存和网络限制。
8. 开发模式热重载和示例模板。
9. 审核机制成熟后再建设扩展市场。

扩展后端作为独立进程运行，清单权限不能完全限制其操作系统权限，因此在线市场必须晚于签名、信任和风险提示机制。

### 4.14 设置中心完善

**优先级：P2　工作量：S～M**

当前普通设置主要只有墨水屏开关。

建议加入：

- 主题：跟随系统、浅色、深色、墨水屏。
- 默认视图、默认排序和每页数量。
- 默认下载格式和下载目录。
- 自动下载、仅 Wi-Fi 下载和并发任务数。
- 阅读进度同步开关与冲突策略。
- 自动检查更新和更新频道。
- 代理、请求超时和网络诊断。
- 安全证书策略。
- 数据导出、备份、缓存清理和恢复默认设置。

### 4.15 HTTPS 与自签名证书安全

**优先级：P1　工作量：M**

当前 Tauri 请求默认接受无效证书和主机名，便于连接局域网服务器，但对公网地址风险过高。

建议：

- 默认严格验证 HTTPS。
- 仅在用户明确确认后，按服务器允许自签名证书。
- 首次连接显示证书主机、颁发者、有效期和指纹。
- 证书变化时阻止自动连接并重新确认。
- 对纯 HTTP 公网地址给出明显警告；局域网地址可降低提示强度。
- 将安全选择与单个服务器配置绑定。

### 4.16 账号与隐私

**优先级：P2　工作量：S～M**

建议：

- 编辑昵称、邮箱、头像、密码和设备信息。
- 查看登录状态和会话过期提示。
- 提供本地应用锁和系统生物识别入口。
- 导出个人阅读数据和删除本地缓存。
- 清楚说明哪些数据位于 Talebook、哪些仅保存在本机、哪些由 Readest 管理。
- 调试日志导出前自动脱敏服务器凭证、Cookie 和访问码。

### 4.17 无障碍与键盘操作

**优先级：P2　工作量：M**

建议：

- 所有图标按钮提供可读名称。
- 为菜单、弹窗、选择器和批量模式补齐焦点管理。
- 支持仅键盘完成搜索、切换页面、选择书籍和执行操作。
- 增加跳到主内容、清晰焦点环和屏幕阅读器状态播报。
- 校验颜色对比度、文字缩放和减少动态效果。
- 触控区域不小于 44×44 CSS 像素。

### 4.18 性能与大书库体验

**优先级：P2　工作量：M～L**

建议：

- 大列表使用虚拟滚动或服务端分页，避免一次渲染全部书籍。
- 封面请求增加内存/磁盘缓存和并发限制。
- 搜索输入防抖并取消过期请求。
- 避免历史页面逐本请求状态。
- 批量操作设置合理并发上限，而非一次发出所有请求。
- 记录页面首屏、搜索、封面和下载性能指标。
- 对 1 万、5 万和 10 万册书库建立基准场景。

### 4.19 数据可靠性与迁移

**优先级：P1　工作量：M**

建议：

- 为 Zustand、IndexedDB 和离线记录增加显式 schema 版本。
- 升级应用时执行可回滚的数据迁移。
- 启动时扫描孤立文件、损坏记录和缺失文件。
- 提供“修复离线书库”工具。
- 阅读进度和下载队列使用幂等写入。
- 应用异常退出后恢复未完成任务。
- 在删除、覆盖和迁移前提供可恢复备份。

### 4.20 用户引导与产品文档

**优先级：P2　工作量：S**

建议：

- 首次连接后用三步引导说明书架、下载和阅读。
- 首次进入阅读器时提示 Readest 已提供的 TTS、标注、词典和快捷键。
- 设置页增加“连接诊断”和“导出问题报告”。
- README 明确 Web、桌面、Android 和 iPadOS 的能力差异。
- 建立服务端最低版本和功能兼容表。
- 发布说明按新增、修复、兼容性和已知问题分类。

## 5. 工程质量建议

这些不是直接面向用户的功能，但会决定上述功能能否稳定交付。

### 5.1 建立最小测试体系（基础阶段已完成）

根项目现已接入可直接运行的基础测试，当前覆盖：

- API 绝对地址校验、成功响应、HTTP 错误、业务错误和无效响应。
- 统一错误提示映射与兼容错误码。
- Web/Tauri 平台判断和 Tauri 请求安全配置。
- 离线书籍保存、读取、覆盖、删除和文件名清理。
- 同一本书在多个服务器之间的数据隔离。

后续继续补充：

- 认证、能力协商及新旧 Talebook 响应的完整契约测试。
- 书架、阅读状态和精确进度的契约测试。
- 离线数据库迁移及 Tauri 本地文件写入、删除测试。
- 多格式离线文件、搜索筛选、批量选择和移动端导航测试。

### 5.2 CI 质量门禁（已完成）

现已增加独立的 GitHub Actions CI，在分支推送、面向 `main` 的 PR 和手动触发时执行：

- 冻结锁文件安装依赖。
- Moke 基础测试。
- ESLint 静态检查。
- TypeScript 类型检查。
- 关键 Rust library 测试。
- Linux 环境下的最小 Tauri 桌面构建冒烟测试。
- 使用 Chromium 手机、平板和桌面视口的页面端到端冒烟测试，并上传截图产物。

移动端检查目前覆盖响应式页面行为和截图，不等同于原生 Android/iOS 打包测试；后续如接入对应构建环境，再补充原生平台矩阵。

### 5.3 统一 API 层

所有服务端调用应从页面迁移到 [`src/lib/api.ts`](src/lib/api.ts) 或领域 API 模块：

- `booksApi`
- `readingApi`
- `downloadsApi`
- `accountApi`
- `devicesApi`
- `serverCapabilitiesApi`

页面不应直接拼接端点或解析不同版本的响应。

### 5.4 可观测性

建议：

- 为请求生成关联 ID。
- 区分网络、认证、权限、服务端和本地文件错误。
- 增加下载、同步和扩展事件日志。
- 一键导出脱敏诊断包。
- 对扩展崩溃和阅读器启动失败提供用户可理解的恢复入口。

## 6. 不建议近期重复建设的功能

以下能力应尽量复用 Readest，而不是在 Moke 主应用重写：

- EPUB/PDF 排版引擎。
- TTS 和语音控制。
- 标注、划线、笔记编辑器。
- 翻译、词典和百科查询。
- 阅读主题、字体和翻页方式。
- 双书对照和阅读辅助工具。

Moke 更适合提供：入口、配置透传、状态桥接、Talebook 同步和统一的数据管理。

同样，不建议在签名、权限提示和进程隔离成熟前直接开放公共扩展市场。

## 7. 推荐版本路线

### 0.1.x：稳定性与一致性

- ~~修复搜索格式筛选。~~
- ~~统一错误状态和重试。~~
- ~~增加服务端能力协商。~~
- ~~完成移动端导航和核心页面响应式布局。~~
- ~~补 API、离线存储和平台分支的基础测试。~~

### 0.2：阅读闭环

- ~~Talebook 精确进度与 Readest 双向同步。~~
- 自动在读、手动读完和继续阅读。
- 收藏、在读、读完视图。
- 使用原生统计接口重做个人面板。
- 离线待同步队列和冲突处理。

### 0.3：内容与设备管理

- 下载管理中心。
- Web 模式离线阅读。
- 普通/分片上传。
- 设备管理和批量推送。
- 数据迁移与离线修复工具。

### 0.4：书库组织

- 多服务器。
- 智能书架和组合过滤。
- 搜索建议、相关推荐和网络书源体验升级。
- OPDS/WebDAV 配置助手。

### 0.5：扩展生态

- 应用内扩展安装和更新。
- 签名、发布者身份和权限重授权。
- 扩展日志、资源限制和崩溃恢复。
- 审核流程成熟后再考虑扩展市场。

## 8. 优先级总表

| 建议 | 用户价值 | 工作量 | 服务端依赖 | 推荐优先级 |
|---|---:|---:|---|---:|
| 搜索格式筛选修复 | 高 | S | 无 | P0 |
| 移动端响应式布局 | 高 | M | 无 | P0 |
| 服务端能力协商 | 高 | M | 可选 | P0 |
| 精确阅读进度同步 | 极高 | M | 已具备 | P0 |
| 统一错误与重试 | 高 | S～M | 无 | P1 |
| 阅读状态与收藏体系 | 高 | S～M | 已具备 | P1 |
| 阅读统计与继续阅读 | 高 | S～M | 基础已具备 | P1 |
| 离线下载管理中心 | 极高 | M～L | 部分 | P1 |
| 数据迁移与修复 | 高 | M | 无 | P1 |
| Web 离线阅读 | 高 | M | 无 | P1 |
| 分片上传 | 高 | M | 已具备 | P1 |
| 设备管理与推送 | 高 | M | 已具备 | P1 |
| HTTPS 安全策略 | 高 | M | 无 | P1 |
| 多服务器 | 高 | M | 无 | P1 |
| 智能书架 | 中高 | M | 可选 | P2 |
| 搜索与发现升级 | 中高 | M～L | 部分 | P2 |
| 网络书源体验 | 中高 | M | 已具备 | P2 |
| OPDS/WebDAV 助手 | 中 | M | 已具备 | P2 |
| 设置中心完善 | 中高 | S～M | 无 | P2 |
| 账号与隐私 | 中 | S～M | 部分 | P2 |
| 无障碍与键盘操作 | 中高 | M | 无 | P2 |
| 大书库性能 | 高 | M～L | 部分 | P2 |
| 扩展系统产品化 | 中高 | L | 无 | P2 |
| 正文全文索引 | 中 | L | 需要 | P3 |
| Kobo 深度同步 | 中 | L | 需要 | P3 |

## 9. 外部产品参考

- [Readest Documentation](https://readest.com/docs)：阅读、标注、TTS、翻译、OPDS 和同步能力。
- [Readest Sync & Backup](https://readest.com/docs/sync)：进度、书签、标注、笔记和阅读统计同步。
- [Komga](https://komga.org/)：集合、阅读列表、OPDS 和多客户端阅读。
- [Komga Kobo Sync](https://komga.org/docs/guides/kobo/)：Kobo 双向进度同步。
- [Kavita](https://www.kavitareader.com/)：智能集合、筛选、全文索引和阅读进度。
- [Kavita Smart Filters](https://wiki.kavitareader.com/guides/features/filtering/)：组合条件与动态书架。
- [Calibre-Web](https://github.com/janeczku/calibre-web)：自定义书架、一键推送和 Kobo 同步。

## 10. 最终建议

如果只能选择一条近期主线，应选择：

> **精确阅读进度同步 → 继续阅读 → 阅读状态与统计 → 离线管理。**

这条主线能最大程度利用新版 Talebook 和已经嵌入的 Readest，把 Moke 从“可以浏览和打开书籍的客户端”提升为“真正可长期使用的个人阅读中心”。
